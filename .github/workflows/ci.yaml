name: CI
on:
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Setup Bun
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      # Install dependencies from root
      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Install Chromium
        run: ./node_modules/.bin/playwright install --with-deps chromium

      - name: Run unit tests
        run: bun run test

      - name: Run Deterministic Audio Tests
        run: bun run avt:report

      - name: Upload AVT Artifacts
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: avt-audio-${{ github.run_number }}
          path: avt/output/
          retention-days: 30

      - name: Run Visual Tests (Docker)
        env:
          RTGL_VT_DEBUG: true
          RTGL_VT_CONCURRENCY: 4
        run: bun run vt:report

      - name: Upload VT Images as Artifacts
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: vt-images-${{ github.run_number }}
          path: .rettangoli/
          include-hidden-files: true
          retention-days: 30
